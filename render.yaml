services:
  - type: web
    name: telegram-sender
    env: python
    plan: free  # Use free tier
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn --bind 0.0.0.0:$PORT app:app"  # Binds to Render's PORT env var
    autoDeploy: true  # Auto-deploy on push
